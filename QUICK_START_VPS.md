# Быстрый старт: Выгрузка данных с VPS

Инструкция на 5 минут для загрузки всех данных с VPS.

---

## ⚡ 3 команды для полной выгрузки

```bash
# 1. Установите параметры VPS
export VPS_USER="ваш_пользователь"        # Например: kipol
export VPS_HOST="IP_или_домен"            # Например: metateks.vlch.dev
export VPS_PATH="/home/mt/metateks-dev"   # Путь к проекту на VPS

# 2. Запустите автоматическую выгрузку
./scripts/full_vps_download.sh

# 3. Дождитесь завершения и следуйте инструкциям на экране
```

**Скрипт автоматически:**
- ✅ Подключится к VPS
- ✅ Проверит что там есть
- ✅ Создаст дамп БД
- ✅ Скачает БД и все медиа-файлы
- ✅ Предложит скачать статику, конфиги, логи
- ✅ Создаст папку `vps_backup_ДАТА_ВРЕМЯ/` со всеми данными

---

## 📋 Что получите

После выполнения скрипта у вас будет:

```
vps_backup_20251225_183045/
├── metateks_dump_ДАТА.json  ← База данных (все таблицы)
├── media/                    ← Все медиа-файлы
│   ├── banners/              ← Баннеры CMS
│   ├── news/                 ← Фото новостей
│   ├── models/photos_1c/     ← Фото товаров (если были)
│   └── ...
├── static/                   ← Статика (если выбрали)
├── assets/                   ← Assets (если выбрали)
├── config/                   ← Конфиги для справки
│   ├── .env                  
│   ├── conf/
│   └── requirements*.txt
├── logs_vps/                 ← Логи (если выбрали)
└── README.txt                ← Инструкции что делать дальше
```

---

## 🚀 Загрузка в Docker (после выгрузки)

```bash
# Перейдите в проект
cd /mnt/c/_KIPOL/_WORK/_metatecks/

# Скопируйте медиа в проект
cp -r vps_backup_*/media/* ./media/

# Запустите Docker
docker-compose up -d

# Дождитесь готовности БД (30-60 секунд)
docker-compose exec db pg_isready -U metateks

# Загрузите дамп БД
docker-compose exec -T web python manage.py loaddata < vps_backup_*/metateks_dump_*.json

# Проверьте данные
docker-compose exec web python manage.py shell << 'PYEOF'
from apps.users.models import User
from apps.orders.models import Order
print(f"✓ Пользователей: {User.objects.count()}")
print(f"✓ Заказов: {Order.objects.count()}")
PYEOF
```

---

## ✅ Проверка что всё работает

```bash
# 1. Проверьте что сайт открывается
curl http://localhost/

# 2. Войдите в админку
# Откройте в браузере: http://localhost/admin/
# Используйте email/пароль с VPS

# 3. Проверьте медиа-файлы
curl -I http://localhost/media/

# 4. Проверьте статус контейнеров
docker-compose ps
# Все должны быть "Up" и "healthy"
```

---

## 🚨 Если что-то пошло не так

### Ошибка SSH подключения

```bash
# Проверьте доступ вручную
ssh $VPS_USER@$VPS_HOST

# Если просит пароль каждый раз - настройте SSH ключ
ssh-copy-id $VPS_USER@$VPS_HOST
```

### Ошибка при loaddata

```bash
# Очистите БД и попробуйте снова
docker-compose exec web python manage.py flush --noinput
docker-compose exec web python manage.py migrate
docker-compose exec -T web python manage.py loaddata < vps_backup_*/metateks_dump_*.json
```

### Медиа не отображаются

```bash
# Проверьте права доступа
chmod -R 755 media/
find media/ -type f -exec chmod 644 {} \;

# Перезапустите nginx
docker-compose restart nginx
```

---

## 📚 Подробная документация

Если нужны детали или возникли проблемы:

- **[VPS_FULL_BACKUP.md](docs/VPS_FULL_BACKUP.md)** - Подробная инструкция со всеми вариантами
- **[VPS_QUICK_DOWNLOAD.md](docs/VPS_QUICK_DOWNLOAD.md)** - Шпаргалка команд
- **[scripts/README.md](scripts/README.md)** - Описание всех скриптов

---

## 💡 Полезные команды

### Проверить что есть на VPS (БЕЗ загрузки)

```bash
export VPS_USER="ваш_пользователь"
export VPS_HOST="IP_VPS"
./scripts/check_vps_data.sh
```

### Узнать размеры до загрузки

```bash
ssh $VPS_USER@$VPS_HOST "cd $VPS_PATH && du -sh media/ static/ logs/"
```

### Загрузить только медиа (без БД)

```bash
rsync -avz --progress $VPS_USER@$VPS_HOST:$VPS_PATH/media/ ./media/
```

---

## 🎯 Что дальше?

После успешной миграции:

1. **Настройте 1С интеграцию:**
   - URL: `http://localhost/cml/1c_exchange.php`
   - Документация: `docs/1C_INTEGRATION.md`

2. **Проверьте CMS:**
   - Откройте: `http://localhost/admin/`
   - Создайте контент: `docs/CMS_GUIDE.md`

3. **Настройте production:**
   - Смените пароли: `docs/DOCKER_SECURITY.md`
   - Настройте домены: `docs/URL_STRUCTURE.md`

---

**Время выполнения:** 5-30 минут (зависит от размера медиа)  
**Требуется:** SSH доступ к VPS, 10+ GB свободного места
