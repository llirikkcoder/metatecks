# Нужно ли переносить базу данных с VPS?

## 🤔 Короткий ответ

**Зависит от того, что у вас есть на VPS!**

- ❌ **Каталог товаров переносить НЕ нужно** - он придет из 1С автоматически
- ⚠️ **Пользователи, заказы, CMS контент - НУЖНО переносить!**

---

## 📊 Таблица: Что переносить, а что нет

| Данные | Источник | Переносить? | Почему |
|--------|----------|-------------|---------|
| **Товары** | 1С | ❌ НЕТ | Придут из 1С при синхронизации |
| **Категории** | 1С | ❌ НЕТ | Придут из 1С |
| **Цены** | 1С | ❌ НЕТ | Придут из 1С |
| **Изображения товаров** | 1С | ❌ НЕТ | Загрузятся из 1С |
| **Остатки на складах** | 1С | ❌ НЕТ | Придут из 1С |
| **Характеристики** | 1С | ❌ НЕТ | Придут из 1С |
| **Бренды** | 1С | ❌ НЕТ | Придут из 1С |
| | | | |
| **Пользователи** | Сайт | ✅ ДА | Регистрации на сайте |
| **Заказы** | Сайт | ✅ ДА | История заказов клиентов |
| **CMS контент** (страницы, новости) | Сайт | ✅ ДА | Контент создан вручную |
| **Баннеры** | Сайт | ✅ ДА | Настроены вручную |
| **Промо-акции** | Сайт | ✅ ДА | Созданы вручную |
| **Города/склады** | Сайт | ⚠️ ВОЗМОЖНО | Если настроены вручную |
| **Настройки сайта** | Сайт | ⚠️ ВОЗМОЖНО | Кастомные настройки |

---

## 🔍 Как проверить, что нужно переносить

### Автоматическая проверка (рекомендуется)

```bash
# Настройте доступ к VPS
export VPS_USER="your_username"
export VPS_HOST="your_vps_ip"
export VPS_PATH="/home/mt/metateks-dev"

# Запустите проверку
./scripts/check_vps_data.sh
```

Скрипт покажет:
- ✅ Что можно НЕ переносить (придет из 1С)
- ⚠️ Что НУЖНО перенести (создано на сайте)
- 💡 Рекомендацию: нужна ли миграция БД

### Ручная проверка

Подключитесь к VPS и проверьте:

```bash
ssh user@vps
cd /home/mt/metateks-dev
source ~/.virtualenvs/metateks/bin/activate

python manage.py shell
```

```python
from apps.users.models import User
from apps.orders.models import Order
from apps.content.models import Page, News

# Пользователи
print(f"Пользователей: {User.objects.count()}")

# Заказы
print(f"Заказов: {Order.objects.count()}")

# CMS контент
print(f"Страниц: {Page.objects.count()}")
print(f"Новостей: {News.objects.count()}")
```

**Если хотя бы одно число > 0** → нужно переносить БД!

---

## 🎯 Сценарии использования

### Сценарий 1: У вас есть пользователи и заказы на VPS

**Ситуация:**
- Сайт работает
- Есть зарегистрированные пользователи
- Есть история заказов
- Есть CMS контент

**Рекомендация:**
```bash
# ✅ ОБЯЗАТЕЛЬНО перенесите БД
./scripts/migrate_from_vps.sh

# После переноса запустите синхронизацию с 1С
# Каталог товаров обновится автоматически
```

**Результат:**
- ✅ Все пользователи и заказы сохранены
- ✅ CMS контент работает
- ✅ Каталог товаров обновлен из 1С

---

### Сценарий 2: Это новая установка, данных нет

**Ситуация:**
- Сайт только настроили
- Пользователей нет
- Заказов нет
- CMS контент не наполнен

**Рекомендация:**
```bash
# ✅ Можно НЕ переносить БД
# Просто запустите синхронизацию с 1С

# 1. Создайте пользователя для 1С
docker-compose exec web python manage.py createsuperuser

# 2. Настройте интеграцию с 1С
# См. docs/1C_INTEGRATION.md

# 3. Запустите обмен в 1С
# Каталог загрузится автоматически
```

**Результат:**
- ✅ Чистая база
- ✅ Каталог товаров из 1С
- ✅ Быстрый старт

---

### Сценарий 3: Нужен только каталог, остальное не важно

**Ситуация:**
- Хотите получить только каталог товаров
- Старые заказы не нужны
- CMS контент перенаполните заново

**Рекомендация:**
```bash
# ✅ Не переносите БД
# Начните с чистой базы + синхронизация с 1С

# Преимущества:
# - Чистая БД без мусора
# - Актуальные данные из 1С
# - Нет проблем совместимости
```

**Недостатки:**
- ❌ Потеряете историю заказов
- ❌ Пользователи должны зарегистрироваться заново
- ❌ CMS контент нужно создать заново

---

## ⚡ Быстрое решение

### Вопрос 1: Есть ли у вас зарегистрированные пользователи?

- **ДА** → Переносите БД (пользователи не смогут войти без переноса)
- **НЕТ** → Можно не переносить

### Вопрос 2: Есть ли важная история заказов?

- **ДА** → Переносите БД (история заказов нужна)
- **НЕТ** → Можно не переносить

### Вопрос 3: Есть ли важный CMS контент?

- **ДА** → Переносите БД (страницы, новости)
- **НЕТ** → Можно не переносить

**Если хотя бы один раз ответили "ДА"** → переносите БД!

---

## 📋 План действий

### Вариант А: Переносите БД (есть важные данные)

```bash
# 1. Проверьте данные на VPS
./scripts/check_vps_data.sh

# 2. Перенесите БД
./scripts/migrate_from_vps.sh

# 3. Синхронизируйте каталог с 1С
# Каталог обновится автоматически при обмене
```

**Время:** ~30-60 минут (зависит от размера БД)

---

### Вариант Б: НЕ переносите БД (начинаете с нуля)

```bash
# 1. Убедитесь, что контейнеры запущены
docker-compose up -d

# 2. Создайте суперпользователя
docker-compose exec web python manage.py createsuperuser

# 3. Создайте пользователя для 1С
# (можно использовать того же)

# 4. Дайте права cml.add_exchange в админке
# http://localhost/admin/

# 5. Настройте 1С на http://localhost/cml/1c_exchange.php

# 6. Запустите обмен в 1С
# Каталог загрузится автоматически
```

**Время:** ~10-15 минут

---

## 🔄 Что произойдет при синхронизации с 1С

### При первом обмене 1С → Django:

```
1С отправляет:
├── import.xml (каталог товаров)
│   ├── Группы (категории)
│   ├── Свойства (характеристики)
│   ├── Товары
│   └── Картинки
├── offers.xml (цены и остатки)
│   ├── Цены
│   └── Остатки на складах
└── Изображения (.jpg, .png)
    ├── photo1.jpg
    ├── photo2.jpg
    └── ...

Django обрабатывает:
├── Парсит XML файлы
├── Сохраняет в промежуточные таблицы (ImportedProduct, и т.д.)
├── Синхронизирует с основными таблицами (Product, Category, и т.д.)
├── Копирует изображения в media/models/photos_1c/
└── Обновляет количество в наличии

Результат:
✅ Полный каталог товаров
✅ Актуальные цены
✅ Остатки на складах
✅ Изображения товаров
```

**Время первой синхронизации:**
- 100 товаров: ~1-2 минуты
- 1000 товаров: ~5-10 минут
- 10000 товаров: ~30-60 минут

---

## 💡 Рекомендации

### Если сомневаетесь

1. **Сделайте резервную копию VPS БД** (на всякий случай):
   ```bash
   ssh user@vps
   cd /home/mt/metateks-dev
   pg_dump -U metateks -d metateks -F c -f backup_$(date +%Y%m%d).backup
   ```

2. **Проверьте данные скриптом**:
   ```bash
   ./scripts/check_vps_data.sh
   ```

3. **Примите решение** на основе вывода скрипта

### Если хотите "все и сразу"

```bash
# Перенесите БД + синхронизация с 1С
./scripts/migrate_from_vps.sh

# После переноса каталог обновится из 1С
# Пользователи и заказы сохранятся
```

---

## 🎓 Что важно понимать

### 1С ≠ Полная база данных

1С синхронизирует **только каталог товаров**:
- ✅ Товары
- ✅ Цены
- ✅ Остатки
- ✅ Изображения

1С **НЕ** синхронизирует:
- ❌ Пользователей сайта
- ❌ Заказы (только отправляет В 1С, не получает обратно)
- ❌ CMS контент
- ❌ Настройки сайта

### База данных > Каталог из 1С

База данных содержит:
- Каталог из 1С (обновляется автоматически)
- + Пользователей (создаются на сайте)
- + Заказы (создаются на сайте)
- + CMS контент (создается вручную)
- + Настройки (настраиваются вручную)

---

## ✅ Итоговая таблица решений

| Ситуация | Решение | Команда |
|----------|---------|---------|
| Есть пользователи/заказы | ✅ Переносить БД | `./scripts/migrate_from_vps.sh` |
| Только тестовые данные | ❌ Не переносить | Начать с чистой БД + 1С |
| Важный CMS контент | ✅ Переносить БД | `./scripts/migrate_from_vps.sh` |
| Пустой сайт, только 1С | ❌ Не переносить | Только синхронизация с 1С |
| Не уверен | ✅ Проверить | `./scripts/check_vps_data.sh` |

---

## 📞 Дополнительная помощь

- **Проверка данных:** `./scripts/check_vps_data.sh`
- **Миграция БД:** См. [DATABASE_MIGRATION.md](DATABASE_MIGRATION.md)
- **Интеграция с 1С:** См. [1C_INTEGRATION.md](1C_INTEGRATION.md)
- **Мониторинг 1С:** См. [1C_MONITORING.md](1C_MONITORING.md)
